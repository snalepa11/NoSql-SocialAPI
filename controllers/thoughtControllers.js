const { Thought, User } = require("../models");

const thoughtController = {
  // get all Thoughts
  // do all routing for this get, get byID, put, post, delete. then do add and remove reaction
};
  module.exports = thoughtController;